# AI setup
Set `OPENAI_API_KEY` and an approved `OPENAI_MODEL`. Without a key, the public flow gracefully constructs a basic editable draft from the customer's own words. Prompts expressly prohibit invented facts. Apply tenant entitlements, budgets, moderation, retention policy, and regional privacy review before broad rollout.
