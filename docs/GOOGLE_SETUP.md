# Google setup
Create a Google Cloud project, enable Places API and (after approval) Business Profile APIs, then configure OAuth redirect URLs under the production HTTPS domain. Set `GOOGLE_MAPS_API_KEY`, `GOOGLE_CLIENT_ID`, and `GOOGLE_CLIENT_SECRET`. Use official APIs only; ReviewAI does not scrape. A write-review URL may be set per location while OAuth is disconnected.
