import{requireSession}from"@/lib/auth";export default async function Page(){const s=await requireSession();return <><h2>Analytics</h2><div className="card"><h3>Tenant workspace</h3><p className="muted">This module is securely scoped to {s.membership.tenant.name}. Features become available when their required connection, data, permission, or plan entitlement is present.</p></div></>}
