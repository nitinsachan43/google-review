"use client";import {useState}from"react";import{useRouter}from"next/navigation";export function CreateBusiness(){const[open,setOpen]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const res=await fetch("/api/businesses",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(res.ok){setOpen(false);router.refresh()}}return <div className="card" style={{marginBottom:18}}>{!open?<button onClick={()=>setOpen(true)}>Add business</button>:<form className="form" onSubmit={submit}><label>Business name<input name="name" required/></label><label>Industry<select name="industry"><option>Restaurant</option><option>Salon</option><option>Clinic</option><option>Retail</option><option>Professional Services</option><option>Other</option></select></label><label>City<input name="city"/></label><button>Create business</button></form>}</div>}
